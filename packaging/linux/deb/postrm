#!/bin/bash
# Post-removal script for simple-nfsd

set -e

PROJECT_NAME="simple-nfsd"
CONFIG_DIR="/etc/$simple-nfsd"
LOG_DIR="/var/log/$simple-nfsd"

if [ "$1" = "purge" ]; then
    # Remove configuration files (optional - user may want to keep them)
    # Uncomment to remove config on purge:
    # rm -rf "$CONFIG_DIR"
    
    # Remove log files
    rm -rf "$LOG_DIR"
    
    # Remove service user (optional)
    # userdel nfsddev 2>/dev/null || true
fi

exit 0

